spring:
  application:
    name: stock-service
  # profiles:
  #   active: ${ACTIVE_PROFILE:dev}
  cloud:
    config:
      uri: ${CONFIG_URI:http://localhost:8888}
      fail-fast: ${FAIL_FAST:false}
      password: 123456
      username: user