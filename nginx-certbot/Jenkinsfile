pipeline {

    agent any

    stages {

        stage('Build') {
            steps {
                sh '''
                    ./init-letsencrypt.sh
                '''
            }
        }
    }
}
